
&НаКлиенте
Процедура Показать(Команда)
Номенклатура = Элементы.Список.ТекущаяСтрока;
Если Номенклатура = Неопределено Тогда Возврат; КонецЕсли;
ПараметрФормы = Новый Структура("Номенклатура", Номенклатура);
ФормаСпискаОтбор = ОткрытьФорму("Документ.ЗакупкаТоваров.Форма.ФормаОтбор", ПараметрФормы);
КонецПроцедуры

&НаСервере
Процедура ПоказатьНаСервере()
	// Вставить содержимое обработчика.
КонецПроцедуры
